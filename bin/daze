#! /bin/bash

function DPConnected {
	! xrandr | grep -c "DP-1 disconnected"
}

function enableDP {
	if $1
	then
		echo yeah
		xrandr --output DP-1 --auto --primary --output LVDS-1 --auto --left-of DP-1
	else
		echo nope
		xrandr --output LVDS-1 --auto
	fi
}

enableDP DPConnected
